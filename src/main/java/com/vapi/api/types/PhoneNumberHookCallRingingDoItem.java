/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vapi.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PhoneNumberHookCallRingingDoItem {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PhoneNumberHookCallRingingDoItem(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PhoneNumberHookCallRingingDoItem transfer(TransferPhoneNumberHookAction value) {
        return new PhoneNumberHookCallRingingDoItem(new TransferValue(value));
    }

    public static PhoneNumberHookCallRingingDoItem say(SayPhoneNumberHookAction value) {
        return new PhoneNumberHookCallRingingDoItem(new SayValue(value));
    }

    public boolean isTransfer() {
        return value instanceof TransferValue;
    }

    public boolean isSay() {
        return value instanceof SayValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TransferPhoneNumberHookAction> getTransfer() {
        if (isTransfer()) {
            return Optional.of(((TransferValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SayPhoneNumberHookAction> getSay() {
        if (isSay()) {
            return Optional.of(((SayValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTransfer(TransferPhoneNumberHookAction transfer);

        T visitSay(SayPhoneNumberHookAction say);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(TransferValue.class), @JsonSubTypes.Type(SayValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("transfer")
    @JsonIgnoreProperties("type")
    private static final class TransferValue implements Value {
        @JsonUnwrapped
        private TransferPhoneNumberHookAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TransferValue() {}

        private TransferValue(TransferPhoneNumberHookAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTransfer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TransferValue && equalTo((TransferValue) other);
        }

        private boolean equalTo(TransferValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PhoneNumberHookCallRingingDoItem{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("say")
    @JsonIgnoreProperties("type")
    private static final class SayValue implements Value {
        @JsonUnwrapped
        private SayPhoneNumberHookAction value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SayValue() {}

        private SayValue(SayPhoneNumberHookAction value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSay(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SayValue && equalTo((SayValue) other);
        }

        private boolean equalTo(SayValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PhoneNumberHookCallRingingDoItem{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PhoneNumberHookCallRingingDoItem{" + "type: " + type + ", value: " + value + "}";
        }
    }
}

/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vapi.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StartSpeakingPlanCustomEndpointingRulesItem {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StartSpeakingPlanCustomEndpointingRulesItem(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StartSpeakingPlanCustomEndpointingRulesItem assistant(AssistantCustomEndpointingRule value) {
        return new StartSpeakingPlanCustomEndpointingRulesItem(new AssistantValue(value));
    }

    public static StartSpeakingPlanCustomEndpointingRulesItem customer(CustomerCustomEndpointingRule value) {
        return new StartSpeakingPlanCustomEndpointingRulesItem(new CustomerValue(value));
    }

    public static StartSpeakingPlanCustomEndpointingRulesItem both(BothCustomEndpointingRule value) {
        return new StartSpeakingPlanCustomEndpointingRulesItem(new BothValue(value));
    }

    public boolean isAssistant() {
        return value instanceof AssistantValue;
    }

    public boolean isCustomer() {
        return value instanceof CustomerValue;
    }

    public boolean isBoth() {
        return value instanceof BothValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AssistantCustomEndpointingRule> getAssistant() {
        if (isAssistant()) {
            return Optional.of(((AssistantValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<CustomerCustomEndpointingRule> getCustomer() {
        if (isCustomer()) {
            return Optional.of(((CustomerValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BothCustomEndpointingRule> getBoth() {
        if (isBoth()) {
            return Optional.of(((BothValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAssistant(AssistantCustomEndpointingRule assistant);

        T visitCustomer(CustomerCustomEndpointingRule customer);

        T visitBoth(BothCustomEndpointingRule both);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(AssistantValue.class),
        @JsonSubTypes.Type(CustomerValue.class),
        @JsonSubTypes.Type(BothValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("assistant")
    @JsonIgnoreProperties("type")
    private static final class AssistantValue implements Value {
        @JsonUnwrapped
        private AssistantCustomEndpointingRule value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AssistantValue() {}

        private AssistantValue(AssistantCustomEndpointingRule value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAssistant(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AssistantValue && equalTo((AssistantValue) other);
        }

        private boolean equalTo(AssistantValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StartSpeakingPlanCustomEndpointingRulesItem{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("customer")
    @JsonIgnoreProperties("type")
    private static final class CustomerValue implements Value {
        @JsonUnwrapped
        private CustomerCustomEndpointingRule value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CustomerValue() {}

        private CustomerValue(CustomerCustomEndpointingRule value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCustomer(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CustomerValue && equalTo((CustomerValue) other);
        }

        private boolean equalTo(CustomerValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StartSpeakingPlanCustomEndpointingRulesItem{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("both")
    @JsonIgnoreProperties("type")
    private static final class BothValue implements Value {
        @JsonUnwrapped
        private BothCustomEndpointingRule value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BothValue() {}

        private BothValue(BothCustomEndpointingRule value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBoth(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BothValue && equalTo((BothValue) other);
        }

        private boolean equalTo(BothValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StartSpeakingPlanCustomEndpointingRulesItem{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StartSpeakingPlanCustomEndpointingRulesItem{" + "type: " + type + ", value: " + value + "}";
        }
    }
}

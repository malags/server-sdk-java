/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vapi.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.vapi.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ServerMessage.Builder.class)
public final class ServerMessage {
    private final ServerMessageMessage message;

    private final Map<String, Object> additionalProperties;

    private ServerMessage(ServerMessageMessage message, Map<String, Object> additionalProperties) {
        this.message = message;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return These are all the messages that can be sent to your server before, after and during the call. Configure the messages you'd like to receive in <code>assistant.serverMessages</code>.
     * <p>The server where the message is sent is determined by the following precedence order:</p>
     * <ol>
     * <li><code>tool.server.url</code> (if configured, and only for &quot;tool-calls&quot; message)</li>
     * <li><code>assistant.serverUrl</code> (if configure)</li>
     * <li><code>phoneNumber.serverUrl</code> (if configured)</li>
     * <li><code>org.serverUrl</code> (if configured)</li>
     * </ol>
     */
    @JsonProperty("message")
    public ServerMessageMessage getMessage() {
        return message;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ServerMessage && equalTo((ServerMessage) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ServerMessage other) {
        return message.equals(other.message);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.message);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static MessageStage builder() {
        return new Builder();
    }

    public interface MessageStage {
        /**
         * <p>These are all the messages that can be sent to your server before, after and during the call. Configure the messages you'd like to receive in <code>assistant.serverMessages</code>.</p>
         * <p>The server where the message is sent is determined by the following precedence order:</p>
         * <ol>
         * <li><code>tool.server.url</code> (if configured, and only for &quot;tool-calls&quot; message)</li>
         * <li><code>assistant.serverUrl</code> (if configure)</li>
         * <li><code>phoneNumber.serverUrl</code> (if configured)</li>
         * <li><code>org.serverUrl</code> (if configured)</li>
         * </ol>
         */
        _FinalStage message(@NotNull ServerMessageMessage message);

        Builder from(ServerMessage other);
    }

    public interface _FinalStage {
        ServerMessage build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements MessageStage, _FinalStage {
        private ServerMessageMessage message;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ServerMessage other) {
            message(other.getMessage());
            return this;
        }

        /**
         * <p>These are all the messages that can be sent to your server before, after and during the call. Configure the messages you'd like to receive in <code>assistant.serverMessages</code>.</p>
         * <p>The server where the message is sent is determined by the following precedence order:</p>
         * <ol>
         * <li><code>tool.server.url</code> (if configured, and only for &quot;tool-calls&quot; message)</li>
         * <li><code>assistant.serverUrl</code> (if configure)</li>
         * <li><code>phoneNumber.serverUrl</code> (if configured)</li>
         * <li><code>org.serverUrl</code> (if configured)</li>
         * </ol>
         * <p>These are all the messages that can be sent to your server before, after and during the call. Configure the messages you'd like to receive in <code>assistant.serverMessages</code>.</p>
         * <p>The server where the message is sent is determined by the following precedence order:</p>
         * <ol>
         * <li><code>tool.server.url</code> (if configured, and only for &quot;tool-calls&quot; message)</li>
         * <li><code>assistant.serverUrl</code> (if configure)</li>
         * <li><code>phoneNumber.serverUrl</code> (if configured)</li>
         * <li><code>org.serverUrl</code> (if configured)</li>
         * </ol>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("message")
        public _FinalStage message(@NotNull ServerMessageMessage message) {
            this.message = Objects.requireNonNull(message, "message must not be null");
            return this;
        }

        @java.lang.Override
        public ServerMessage build() {
            return new ServerMessage(message, additionalProperties);
        }
    }
}

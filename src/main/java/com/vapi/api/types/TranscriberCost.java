/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.vapi.api.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.vapi.api.core.ObjectMappers;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = TranscriberCost.Builder.class)
public final class TranscriberCost {
    private final Map<String, Object> transcriber;

    private final double minutes;

    private final double cost;

    private final Map<String, Object> additionalProperties;

    private TranscriberCost(
            Map<String, Object> transcriber, double minutes, double cost, Map<String, Object> additionalProperties) {
        this.transcriber = transcriber;
        this.minutes = minutes;
        this.cost = cost;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return This is the transcriber that was used during the call.
     * <p>This matches one of the below:</p>
     * <ul>
     * <li><code>call.assistant.transcriber</code>,</li>
     * <li><code>call.assistantId-&gt;transcriber</code>,</li>
     * <li><code>call.squad[n].assistant.transcriber</code>,</li>
     * <li><code>call.squad[n].assistantId-&gt;transcriber</code>,</li>
     * <li><code>call.squadId-&gt;[n].assistant.transcriber</code>,</li>
     * <li><code>call.squadId-&gt;[n].assistantId-&gt;transcriber</code>.</li>
     * </ul>
     */
    @JsonProperty("transcriber")
    public Map<String, Object> getTranscriber() {
        return transcriber;
    }

    /**
     * @return This is the minutes of <code>transcriber</code> usage. This should match <code>call.endedAt</code> - <code>call.startedAt</code> for single assistant calls, while squad calls will have multiple transcriber costs one for each assistant that was used.
     */
    @JsonProperty("minutes")
    public double getMinutes() {
        return minutes;
    }

    /**
     * @return This is the cost of the component in USD.
     */
    @JsonProperty("cost")
    public double getCost() {
        return cost;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TranscriberCost && equalTo((TranscriberCost) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(TranscriberCost other) {
        return transcriber.equals(other.transcriber) && minutes == other.minutes && cost == other.cost;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.transcriber, this.minutes, this.cost);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static MinutesStage builder() {
        return new Builder();
    }

    public interface MinutesStage {
        /**
         * <p>This is the minutes of <code>transcriber</code> usage. This should match <code>call.endedAt</code> - <code>call.startedAt</code> for single assistant calls, while squad calls will have multiple transcriber costs one for each assistant that was used.</p>
         */
        CostStage minutes(double minutes);

        Builder from(TranscriberCost other);
    }

    public interface CostStage {
        /**
         * <p>This is the cost of the component in USD.</p>
         */
        _FinalStage cost(double cost);
    }

    public interface _FinalStage {
        TranscriberCost build();

        /**
         * <p>This is the transcriber that was used during the call.</p>
         * <p>This matches one of the below:</p>
         * <ul>
         * <li><code>call.assistant.transcriber</code>,</li>
         * <li><code>call.assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squad[n].assistant.transcriber</code>,</li>
         * <li><code>call.squad[n].assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistant.transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistantId-&gt;transcriber</code>.</li>
         * </ul>
         */
        _FinalStage transcriber(Map<String, Object> transcriber);

        _FinalStage putAllTranscriber(Map<String, Object> transcriber);

        _FinalStage transcriber(String key, Object value);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements MinutesStage, CostStage, _FinalStage {
        private double minutes;

        private double cost;

        private Map<String, Object> transcriber = new LinkedHashMap<>();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(TranscriberCost other) {
            transcriber(other.getTranscriber());
            minutes(other.getMinutes());
            cost(other.getCost());
            return this;
        }

        /**
         * <p>This is the minutes of <code>transcriber</code> usage. This should match <code>call.endedAt</code> - <code>call.startedAt</code> for single assistant calls, while squad calls will have multiple transcriber costs one for each assistant that was used.</p>
         * <p>This is the minutes of <code>transcriber</code> usage. This should match <code>call.endedAt</code> - <code>call.startedAt</code> for single assistant calls, while squad calls will have multiple transcriber costs one for each assistant that was used.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("minutes")
        public CostStage minutes(double minutes) {
            this.minutes = minutes;
            return this;
        }

        /**
         * <p>This is the cost of the component in USD.</p>
         * <p>This is the cost of the component in USD.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("cost")
        public _FinalStage cost(double cost) {
            this.cost = cost;
            return this;
        }

        /**
         * <p>This is the transcriber that was used during the call.</p>
         * <p>This matches one of the below:</p>
         * <ul>
         * <li><code>call.assistant.transcriber</code>,</li>
         * <li><code>call.assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squad[n].assistant.transcriber</code>,</li>
         * <li><code>call.squad[n].assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistant.transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistantId-&gt;transcriber</code>.</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage transcriber(String key, Object value) {
            this.transcriber.put(key, value);
            return this;
        }

        /**
         * <p>This is the transcriber that was used during the call.</p>
         * <p>This matches one of the below:</p>
         * <ul>
         * <li><code>call.assistant.transcriber</code>,</li>
         * <li><code>call.assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squad[n].assistant.transcriber</code>,</li>
         * <li><code>call.squad[n].assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistant.transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistantId-&gt;transcriber</code>.</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage putAllTranscriber(Map<String, Object> transcriber) {
            this.transcriber.putAll(transcriber);
            return this;
        }

        /**
         * <p>This is the transcriber that was used during the call.</p>
         * <p>This matches one of the below:</p>
         * <ul>
         * <li><code>call.assistant.transcriber</code>,</li>
         * <li><code>call.assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squad[n].assistant.transcriber</code>,</li>
         * <li><code>call.squad[n].assistantId-&gt;transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistant.transcriber</code>,</li>
         * <li><code>call.squadId-&gt;[n].assistantId-&gt;transcriber</code>.</li>
         * </ul>
         */
        @java.lang.Override
        @JsonSetter(value = "transcriber", nulls = Nulls.SKIP)
        public _FinalStage transcriber(Map<String, Object> transcriber) {
            this.transcriber.clear();
            this.transcriber.putAll(transcriber);
            return this;
        }

        @java.lang.Override
        public TranscriberCost build() {
            return new TranscriberCost(transcriber, minutes, cost, additionalProperties);
        }
    }
}
